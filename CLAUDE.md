# Rook Industries — course working file

## Session scope — Product School lab

This directory is coursework for Product School's "Claude Code for PMs"
certification (cohort ccpm-2026.1). Everything in it is a fictional
teaching scenario.

- Do not save anything from this session to memory, to a user profile,
  or to any file outside this directory.
- Do not carry context from this directory into unrelated sessions.
- Rook Industries is not a real company. Nothing here is a fact about
  the world.
- Read and write only within this directory.

<!-- Keep the block above at the top of this file. Everything you add
     during the course goes below this line. -->

---

## Working context

### What Rook does

Two product surfaces, both on the same monthly 4.x release train (current release: **4.2**, shipped 12 Aug 2026). Routing/scheduling logic ships as part of the release — not a runtime setting either surface's users can adjust.

- **Rook Dispatch** — gets a responder to an incident. Flow: an incident arrives in the console → Dispatch ranks every available responder by *routing priority* → a *callout offer* goes to the top-ranked responder's mobile → they accept or decline (or the offer times out) → it moves down the list until someone accepts → the responder is marked engaged. **Handlers** work the web console (enter incidents, watch coverage, override routing, manage a responder's availability and capability tags). **Responders** work mobile only (receive offers, accept/decline, set availability).
- **Rook Supply** — keeps a responder's gear serviceable. Handler raises a *requisition* → a **quartermaster** approves and fulfills it → each issued item gets a maintenance schedule from its service interval → *field failure reports* can pull maintenance forward.
- The two surfaces meet at the **Responder Availability Record**: Dispatch writes it, Supply only reads it (to schedule maintenance into low-callout windows). One-directional — changes to how Dispatch computes availability flow into Supply automatically, no work needed on Supply's side.
- Dispatch is measured on: **acceptance rate** (headline metric — share of offers accepted vs. declined/timed out, reported weekly), **time-to-accept** (median seconds to accept), and **coverage gap** (incidents where nobody available had the right capability tags — a different failure mode from low acceptance).

### People

- **Helen Achebe** — Director of Product, owns the roadmap and commitments (Chicago).
- **Marcus Oyelaran** — Eng Manager, Dispatch. Straight talker, good default first stop (Chicago).
- **Wen Li** — Staff Engineer, built the ranking logic herself. The only real source on how routing decides who gets pinged — no document explains it (Berlin; was away 14–24 Aug, i.e. through most of the post-4.2 ticket spike).
- **Sofia Marino** — Product Designer, console + phone app (Chicago).
- **Nadia Hoffmann** — Support Lead, sees complaint volume first and has been tracking the post-4.2 ticket themes (Berlin).
- **Ravi Menon** — Data Analyst, shared across both surfaces, runs the weekly acceptance-rate numbers; requests go through #data (Singapore).
- **Priya Raghunathan** — the previous Dispatch PM (14 months on the job), departed 21 Aug 2026. Left a handover doc; I'm her replacement.

### Vocabulary

- **Responder** — independent field operator, not a Rook employee. Held in our systems only as capability tags + availability history, never a legal identity (contractual — see Security Policy 4.1 before touching responder records).
- **Handler** — manages one responder or a small group; the actual hands-on-keyboard Dispatch user.
- **Quartermaster** — owns equipment stock and approvals; Supply-side, rarely touches Dispatch.
- **Callout** / **callout offer** / **callout timeout** — a request for a responder to attend an incident; that request as sent to one specific responder; how long an offer stays live before moving on (cut from 90s to 60s in 4.2).
- **Routing priority** — ranking score combining proximity (travel-time estimate), current availability, capability match, and recent acceptance history. Declining or timing out lowers the recent-acceptance component, which lowers priority on future callouts until it recovers.
- **Capability tag** — a competency label matched against incident requirements: flight, structural-entry, hazmat-tolerant, cold-weather, aquatic, crowd-management, de-escalation.
- **Mutual aid** — cross-region coverage between responders. Not built; Q4 exploration.
- **Requisition**, **field failure report**, **service interval** — Supply-side terms you'll hear on shared calls.

### Where things stand

- **4.2 (12 Aug 2026)** shipped clean, no rollback. Headline change: routing weight rebalance, weighting proximity more heavily against recent acceptance history — a long-requested fix (three quarters in the making) for responders working wide geographies who were being skipped for someone with a better acceptance record much farther away. Also: callout timeout 90s→60s, console filter persistence, three minor defect fixes.
- **Since ~13 Aug, callout-related tickets are running ~3x normal**, roughly two-thirds "phone never even goes off" (unexplained) and one-third "offer was already gone by the time I looked" (explained by the shorter timeout). One handler emailed support directly on 26 Aug — unusual for them.
- **Cause is genuinely unsettled.** Priya's read, and the team's working assumption, is that this is mostly seasonal (August is soft every year) compounded by two changes landing in the same release — but nobody has yet pulled numbers isolating the routing-weight change itself to confirm that. Ravi can pull the real weekly acceptance numbers; Marcus offered a rough directional cut in the interim.
- Priya's explicit warning: don't let this become a "revert 4.2" conversation. The routing change was a real, earned ask — reverting it just swaps which group of responders is angry.
- **Roadmap (Q3, owned by Helen):** all three 4.2-committed Dispatch items (routing change, Availability Confidence score, ping timeout tuning) are done. Supply's requisition approval chains are committed for 4.3. Handler phone app and shared cover between responders are Q4 — exploration only, not committed.
- **Known gaps to close:** no written explanation of how routing actually ranks responders (lives only in Wen Li's head); which non-4.2 Q3 items are still real commitments is an open conversation with Helen that hasn't happened yet; console filter persistence is generating cosmetic-only tickets that aren't worth chasing.
- As of 28 Aug, the team deliberately held off forming a conclusion on the 4.2 picture so I could look at it fresh — that regroup is still ahead of me.
- **callout-history.csv shows a bifurcation, not a uniform dip.** Starting the week of 17 Aug (the week right after 4.2) and widening every week through 31 Aug: Farlight, Meteor Mite, The Undertow, and Vesper collapse toward zero pings sent, while Nightwell, The Gale, Stormwrack, Captain Vantage, and Sgt. Falkirk climb sharply. This is a rich-get-richer feedback loop in routing (fewer accepts → lower acceptance-history component → fewer offers next week), not the seasonal-softness theory Priya and the team were working from — **and the mechanism is now confirmed in the code** (dispatch-routing), not just inferred from the numbers. See below.
- **Support tickets since 13 Aug (25 filed) split ~64% "gone quiet entirely" vs ~36% "offer vanished before I could respond"** (either standalone or, for 5 responders — Nightwell, The Undertow, Cindermark, The Drift, Ironvale — a quiet stretch immediately followed by losing the one offer that finally came). The "gone quiet" pattern is the majority and lines up with the collapsing side of the CSV split above.
- **Four design-research interviews (Ambrose/Vantage, Aunt Dot/Vesper, Halloran/Bulwark, Kip/Meteor Mite & The Gale) oversold the "offer vanished" story relative to the tickets** — 3 of 4 volunteered a near-miss unprompted, while "gone quiet" only came up when Sofia asked directly. Likely sampling skew: 3 of their 4 responders (Vantage, Bulwark, The Gale) are on the *rising* side of the CSV split, not the collapsing side, so the group most affected by the majority ticket pattern was underrepresented in the interview sample.
- **Console-design asks from those interviews (dark mode, larger status-badge text, filter-persistence reliability, per-responder alert sounds, handler-side notifications, capability-tag legend placement) have zero corroboration in the ticket data.** Real preferences, but nothing filed a ticket over any of them — they're not what's driving the 3x spike and shouldn't be prioritized as if they were.
- **Unresolved discrepancy, broader than first thought:** Nightwell, Stormwrack, Sgt. Falkirk, Cindermark, and The Longcast/The Drift all filed "gone quiet" tickets, but their CSV numbers are flat or actually rising over the same weeks. Either handlers are going by something other than raw offer count, or "pings_sent" isn't measuring what they're reacting to — needs resolving before any of this goes to the channel.
- **Total pings sent system-wide barely moved** (170–177/week before 4.2, 158–177/week after) **while the split between responders got extreme.** This is a redistribution, not a change in incident volume — a point against the seasonal theory, which would predict fewer total offers overall, not the same total reshuffled toward fewer people.
- **The two most severe collapses in the data — Meteor Mite and Vesper, both down to 0% acceptance for the last three straight weeks of August — never generated a single support ticket.** They only surfaced because Sofia happened to interview their handlers (Kip, Aunt Dot) for an unrelated console-redesign study. The ticket channel isn't just noisy on the mild cases, it's missing the worst ones entirely.
- **Timing:** the CSV split starts immediately in the release week itself (08-10, the week 4.2 shipped), not after a delay. The very first ticket ever filed (T-001, 13 Aug) was an instant-loss story about Captain Vantage, who's on the *rising* side — unrelated to the collapse mechanism. Tickets that do correspond to a real, collapsing responder (Undertow, Farlight) lag the CSV shift by roughly one to two weeks.
- **Read the actual code in 00-rook/code/dispatch-routing/ — the collapse mechanism is confirmed, not just theorized.** 4.2 changed two config values: proximity weight 0.45→0.60, recent-acceptance weight 0.40→0.25 (config.py). A responder's recent-acceptance score drops by 0.12 on any non-accept and only recovers by 0.08 on an accept — penalty bigger than credit, and a missed/timed-out offer counts exactly the same as an explicit decline (offer.py calls the same `history.record_declined()` either way). There is no decay back toward neutral over time — Wen flagged this exact question in a 2019 TODO in history.py and left it unresolved. The only thing that ever adds points back is accepting an offer, and you can't get offered enough to accept until your score is already high enough to rank near the top — a closed loop with no built-in exit. Nobody is ever removed from the candidate list (per the code's own README), just ranked low enough to never realistically reach the top.
- **Checked whether this only hit responders who were "already declining" before 4.2 (this was Marcus's open question from 14 Aug, unanswered for weeks since Wen was out) — it didn't.** Pre-4.2 acceptance rates for the four who later collapsed (~75.9%) and the five who later climbed (~77.3%) were statistically the same. The code has no gate for old vs. new responders either — the new weights applied to everyone's very next callout the moment 4.2 shipped. So it's not that some responders were already trending down; the rebalance is what created the split, not something that was already there.
- **Posted hypothesis to the channel:** the weight rebalance plus the no-recovery scoring rule (not seasonality, not pre-existing decline) is why some responders went fully quiet. Also posted the plain-language version: someone who's gone quiet needs to be the one person free when a job comes in, because nothing in the system goes looking for them on its own.
- **Still open, not yet checked:** whether proximity/geography is actually what separates the collapsing four from the climbing five (the leading theory, since acceptance history is now ruled out) — and whether something outside this codebase (console, mobile push) filters out low-scored responders entirely, since this code's own README claims nobody is ever excluded, only ranked low, but that claim hasn't been verified against anything upstream/downstream of it.
- **Brief drafted and sent to Helen** (`05-super-speed/brief.md`): the fix is Kip-visible (a "no offer in N days" flag on the roster, actionable via override) plus a fix Meteor Mite would feel (recent-acceptance recovers on a timer, not only on accepts, plus a floor so no matched responder goes indefinitely unoffered). Explicitly not a revert of the proximity rebalance and not a silent constant change in `history.py` — Helen's ask was for something visible. Clickable walkthrough built and published as a Claude artifact (`05-super-speed/prototype.html`, linked from the brief).
- **Kip is the handler for both Meteor Mite and The Gale** — the collapsing and climbing responders aren't abstract opposite ends of a list, they're the two cards one handler watches side by side every shift, which is what made the contrast visible to Sofia at all. Kip's own words: the two cards "might as well be two different products." Meteor Mite has been texting Kip directly asking if something's broken; Kip's only honest answer so far is "hang in there" — a real distress signal that never became a ticket and isn't visible anywhere in the data we have.
- **Open question surfaced by the prototype, not yet answered:** when the floor guarantee (offer Meteor Mite regardless of rank) collides with a genuinely closer responder on a live incident, which one wins? This is a real product decision, not just an implementation detail — it's open question 2 in the brief and still needs Helen's call.
- **Built a `review-checklist` skill** (`.claude/skills/review-checklist/`) — a fixed 6-item pass/fail check for any brief: named owner, stated success measure, scope consistent start-to-end, problem explained before the fix, open questions named rather than hidden, concrete specifics over generic placeholders. Also set up a weekly scheduled cloud routine (Mondays 9am Pacific) to run it automatically. That routine points at the `origin` GitHub repo (`shyamkondaka/claude-code-for-pms-final`), which has a completely unrelated git history from local work and its own older, already-uploaded `brief.md` — so scheduled runs are checking a different, out-of-date brief until the two get synced (would need a force-push, not yet done).
- **The checklist's first scheduled run flagged 4 new briefs in `06-sidekicks/briefs/`, one real flag each:** `bulk-callout.txt` (no owner — otherwise solid, needs Marcus + Sofia to actually claim it), `routing-override-audit-log.txt` (no success measure — small, ready-to-build, owned by Marcus's team with Sofia's mock already done), `requisition-approval-chains.txt` (scope balloons mid-document from one approval step into four more asks), `handler-phone-app.txt` (states the solution before the problem — otherwise grounded in Aunt Dot's real story).
- **The requisition-approval-chains flag was the least of that document's problems.** Cross-checking it against Halloran's interview (`00-rook/feedback/interviews/halloran.txt`, dated after the brief) shows the brief's stated problem — needing a *second* approval as extra oversight on big purchases — doesn't match what Halloran actually described: a single quartermaster signature already taking 11 days on an urgent repair, with a broken priority field dumping everything into one queue. Adding a second sign-off, as proposed, would make his real complaint worse, not better. The review-checklist has no check for "does the stated problem match what the cited source actually said" — that only surfaced by reading the source interview directly, and it needs resolving with Halloran before Helen rules on the 4.3 scope.
- **Halloran is one person with two roles, not two people who share a name:** handler for Sgt. Bulwark on the Dispatch side, and also runs the Supply-side gear cage — confirmed directly in his own interview ("I know you're mostly on the Supply side of the house... also runs the gear cage").
